<section class="movie">
  <article class="movie-card">
    <img class="movie-card__image" src="{{movie.attributes.image.url}}" alt="Omslag för filmen {{movie.attributes.title}}">
    <h3 class="movie-card__title">{{movie.attributes.title}}</h3>
    <p class="movie-card__intro">{{movie.attributes.intro}}</p>
  </article>
</section>